<library path="kontrolem_ros2_control">
  <class name="kontrolem_ros2_control/KontrolemController"
         type="kontrolem_ros2_control::KontrolemController"
         base_class_type="controller_interface::ControllerInterface">
    <description>
      Kontrol'Em v2 runtime: a ros2_control controller that hosts any
      kontrolem_control::Controller plugin (LQR, QP task-space, ...), mapping
      joint state/command interfaces to the ROS-free control core, with a
      minimal supervisor acting on the controller's status().
    </description>
  </class>
  <class name="kontrolem_ros2_control/FloatingStateProbe"
         type="kontrolem_ros2_control::FloatingStateProbe"
         base_class_type="controller_interface::ControllerInterface">
    <description>
      M3 spike: claims the floating base's scalar &lt;gpio&gt; state interfaces +
      joint states, reassembles a manifold-correct State, and reports base
      height / CoM / quaternion norm. Read-only; tests non-joint state through
      ros2_control.
    </description>
  </class>
  <class name="kontrolem_ros2_control/BaseEstimatorController"
         type="kontrolem_ros2_control::BaseEstimatorController"
         base_class_type="controller_interface::ControllerInterface">
    <description>
      M8: floating-base state estimator wrapper. Claims joint position/velocity +
      per-foot contact interfaces, subscribes to the IMU, runs the ROS-free
      kontrolem_estimation::BaseEstimator (contact-aided, InEKF-ready), and
      publishes the estimated base pose/twist as nav_msgs/Odometry on /base_odom
      for OdometryBaseBridge — the real-robot base source replacing sim ground truth.
    </description>
  </class>
</library>
